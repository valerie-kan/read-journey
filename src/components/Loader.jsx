import { BeatLoader } from "react-spinners";

const Loader = () => {
  return (
    <div style={{ textAlign: "center", padding: "20px" }}>
      <BeatLoader color="#f9f9f9" size={20} />
    </div>
  );
};

export default Loader;
